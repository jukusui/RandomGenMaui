<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Jukusui.RandomGen.View.Control"
             xmlns:vm="clr-namespace:Jukusui.RandomGen.ViewModel"
             xmlns:view="clr-namespace:Jukusui.RandomGen.View"
             x:Class="Jukusui.RandomGen.View.DicePage"
             Title="DicePage" view:ToastManager.Text="{Binding ErrorText}">
    <ContentPage.BindingContext>
        <vm:DiceViewModel/>
    </ContentPage.BindingContext>
    <Grid VerticalOptions="Fill" Margin="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Text="Max" FontSize="Subtitle" VerticalOptions="Center"/>
            <Entry Grid.Column="1" Keyboard="Numeric" Text="{Binding MaximumText,Mode=TwoWay}" ClearButtonVisibility="WhileEditing"></Entry>
            <Stepper Grid.Column="2" Value="{Binding Maximum,Mode=TwoWay}"
                     Minimum="{Binding RangeMin,Mode=OneTime}" Maximum="{Binding RangeMax,Mode=OneTime}"></Stepper>
        </Grid>
        <FlexLayout Grid.Row="1" Padding="-8,-8,-8,0" Wrap="Wrap" >
            <FlexLayout.Resources>
                <Style TargetType="Button" Class="Flex">
                    <Setter Property="Margin" Value="4,4"/>
                    <Setter Property="Command" Value="{Binding SetValueCommand}"/>
                </Style>
            </FlexLayout.Resources>
            <Button StyleClass="Flex" Text="2" CommandParameter="2"/>
            <Button StyleClass="Flex" Text="6" CommandParameter="6"/>
            <Button StyleClass="Flex" Text="10" CommandParameter="10"/>
            <Button StyleClass="Flex" Text="100" CommandParameter="100"/>
        </FlexLayout>
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Text="Roll" FontSize="Subtitle" Command="{Binding RollCommand}"/>
            <ImageButton Grid.Column="1" Command="{Binding DeleteCommand}" VerticalOptions="Center">
                <ImageButton.Source>
                    <FontImageSource Glyph="&#xF518;" FontFamily="MaterialIconsRegular"/>
                </ImageButton.Source>
            </ImageButton>
        </Grid>
        <Border Grid.Row="3">
            <CollectionView ItemsSource="{Binding Results}" SelectionMode="None"
                            ItemsUpdatingScrollMode="KeepLastItemInView" VerticalScrollBarVisibility="Always">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding Key}" FontSize="Small"
                                   VerticalOptions="Center"/>
                            <Entry Grid.Column="1" Text="{Binding Value,Mode=OneWay}" FontSize="Large"
                                   HorizontalOptions="FillAndExpand" IsReadOnly="True"
                                   VerticalOptions="Center">
                                <Entry.GestureRecognizers>
                                    <TapGestureRecognizer Buttons="Primary" Tapped="OnOutputEntryTapped" />
                                </Entry.GestureRecognizers>
                            </Entry>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>
    </Grid>
</ContentPage>